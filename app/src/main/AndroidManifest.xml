<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ulisboa.tecnico.sise.insure.app.activities">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="pt.ulisboa.tecnico.sise.insure.app.GlobalState"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ClaimDetailActivity"></activity>
        <activity android:name=".ClaimHistoryActivity" />
        <activity android:name=".InsuredProfileActivity"/>
        <activity android:name=".NewClaimActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AuthenticationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>